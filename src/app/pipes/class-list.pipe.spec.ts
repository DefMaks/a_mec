import { ClassListPipe } from './class-list.pipe';

describe('ClassListPipe', () => {
  it('create an instance', () => {
    const pipe = new ClassListPipe();
    expect(pipe).toBeTruthy();
  });
});
